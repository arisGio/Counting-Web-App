let first_variable = "hello world" // can be modified
const last_variable = "something" // cannot change it's value

let my_string = "Hello world"
let my_second_string = "24"

let my_number = 15 // number
let my_second_number = "15" // string

let my_array = [1, "hello", "4", "world"] // array

let my_boolean = true // boolean

let my_obj = {  // objects allow you to store data in key-value pairs
    name: "John snow",
    aim: "Learning JavaScript",
    age: 20,
}

// JS interprets everything as an object

let my_button = document.querySelector("#id") // you can even save references to HTML elements in variables

my_button // to use a variable in JS, simply call the variable name like this